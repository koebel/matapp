<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_mat_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="12dp"
    android:paddingTop="6dp">

    <TextView
        android:id="@+id/row_mat_title_firstuse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:gravity="left"
        android:paddingBottom="2dp"
        android:paddingLeft="12dp"
        android:paddingTop="8dp"
        android:text="@string/first_use_title"
        android:textAppearance="?android:textAppearanceMedium"
        android:textColor="?attr/colorPrimaryDark"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/row_mat_desc_firstuse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/row_mat_title_firstuse"
        android:gravity="left"
        android:paddingBottom="12dp"
        android:paddingLeft="12dp"
        android:text="@string/first_use_text" />

</RelativeLayout>
